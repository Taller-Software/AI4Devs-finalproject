[phases.setup]
nixPkgs = ['php84', 'php84Packages.composer']
nixLibs = ['php84Extensions.pdo_mysql', 'php84Extensions.mysqli', 'php84Extensions.mbstring']

[phases.install]
cmds = ['composer install --no-dev --optimize-autoloader']

[start]
cmd = 'frankenphp php-server --listen :${PORT:-8080}'
